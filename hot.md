# Hot — Active Work Tracker

> Auto-updated by agents. Tracks current work and recent history so context carries across sessions.

## Current Task
_No active task._

## Recent History
<!-- Most recent first, keep last 3-5 items -->

| When | What | Status |
|---|---|---|
| 2026-05-05 | **Plan A — Fix `/detail` scoring chain display bug.** `build_scoring_chain` was reading `timeframe_weights` as a flat dict (`.get("daily", 0.2)`, `.get("weekly", 0.8)`), always falling through to 0.2/0.8 and silently dropping monthly. Fixed to use regime-adaptive 3-way weights from `scorer_cfg["timeframe_weights"]` (nested by regime). `scorer_cfg` now flows as a parameter: `handle_detail_command` → `build_full_breakdown` → `build_scoring_chain`. Removed the inline `load_config("scorer")` from inside `build_scoring_chain`. Updated `_insert_score` helper with `monthly_score` param. 7 new tests + 3 updated tests, all 40 detail-command tests green. `verify_pipeline.py`: 0 FAIL, 1 pre-existing WARN. Docs: DESIGN.md §"Regime-Adaptive 3-Way Timeframe Merge" (rewrote the old "Dual Timeframe: 0.2/0.8" section). | ✅ Done |
| 2026-05-05 | **Simplified Telegram signal report** (`src/notifier/formatter.py`, `src/notifier/main.py`). Added `telegram.include_ai_reasoning` boolean flag (default `false`) to `config/notifier.json`. When `false`: Claude API call is skipped entirely in `run_notifier()`; `format_full_report()` omits daily summary, per-ticker reasoning, and market context — showing only header + signal distribution + bullish/bearish/flips concise lines. Section functions accept `include_reasoning: bool = True` param. 65/65 notifier tests pass. Docs: CONFIG.md, DESIGN.md §13.2+§13.4. | ✅ Done |
| 2026-04-29 | **Daily card enriched with `key_signals` top-3, earnings row, and signal-flip badge.** Added three helpers to `src/web/queries.py`. 46 new tests (7+12+9+9+3+3+3), 115 total in `tests/web/` — all pass. Docs: CONFIG.md, DESIGN.md §14.2+§14.6, DEVELOPMENT.md. | ✅ Done |
| 2026-04-28 | **Built read-only web UI** (`src/web/`). FastAPI + Jinja2 + vanilla JS. 75 new tests (all passing), 1653 total tests. Systemd unit, deploy.sh step 12. Docs: README.md, OPERATIONS.md, CONFIG.md, DEVELOPMENT.md, DESIGN.md §14. | ✅ Done |
| 2026-04-29 | **Flipped `weekly_score_method` + `monthly_score_method` to `v2_8cat` in production** after shadow acceptance gate PASS. `verify_pipeline.py` returned 44 PASS / 1 WARN / 0 FAIL. | ✅ Done |
| 2026-04-28 | Commit 9 of weekly/monthly parity (FINAL): notifier-shift documentation + README architecture refresh + DESIGN.md latent-coupling note + hot.md Key Decisions refresh. **Notifier finding**: `ai_reasoner.py:803` includes `weekly_score` (NOT `monthly_score`) in the LLM prompt context, so v2 mode shifts blurb tone subtly. OPERATIONS.md flip procedure now has steps 1b (capture pre-flip blurbs BEFORE step 3 scorer re-run wipes v1 weekly_score values) and 5b (capture post-flip blurbs + diff). README.md scripts section expanded from 9 to 16 entries (all 4 migrations + acceptance gate + analyze script + parity references). DESIGN.md §13.1 documents the LLM-prompt latent coupling. hot.md Key Decisions refreshed to absorb the v1/v2 mode + Option A (separate scores_weekly/_monthly tables) + Option C (no per-timeframe signal classification) decisions from commits 5-6. No code changes. | ✅ Done |
| 2026-04-28 | Commit 8 of weekly/monthly parity: verify_pipeline parity checks. Added 13 new checks to `src/backfiller/verify_pipeline.py`: `weekly_pattern_count` + `monthly_pattern_count` (zero-window + structural high-bound), `weekly_divergence_count` + `monthly_divergence_count` (zero-window only — B3 dropped upper bound), `weekly_crossover_count` + `monthly_crossover_count` (zero-window), `scores_weekly_table_coverage` + `scores_monthly_table_coverage` (B1: INNER JOIN against indicators_*, warm-up tickers excluded), `scores_weekly_score_range` + `scores_monthly_score_range` (composite + 8 categories in [-100,100] via SQL `IS NOT NULL`), `scores_weekly_category_math` + `scores_monthly_category_math` (B2: clamp(sum × expansion) within ±0.01, v1/v2 dual-mode tolerance — passes if either weight set matches; window-bounded by `category_math_window_days`), `monthly_indicator_coverage` (parity gap closer), `no_open_period_persisted` (uses `is_week_closed` / `is_month_closed` from `period_gate`). B4: Renamed existing `check_monthly_score_coverage` → `check_monthly_score_column_coverage` (it checks `scores_daily.monthly_score` column, not the new `scores_monthly` table) and updated all wiring. Pairs 9+10 and 11+12 share private helpers `_check_score_range_for_table` and `_check_category_math_for_table` for under-50-line compliance. New section in orchestrator: `# Period integrity checks` (C10). 11 new config keys in `config/verify_pipeline.json` (all documented in CONFIG.md). 45 new tests added (151 total in `test_verify_pipeline.py`); 1281 non-notifier tests pass overall (baseline 1236 + 45 = 1281). All 45 CheckResult.name values unique. CONFIG.md, OPERATIONS.md, DEVELOPMENT.md updated. | ✅ Done |
| 2026-04-28 | Commit 7 of weekly/monthly parity: calibrator acceptance gate. New `src/scorer/acceptance_gate.py` (pure helpers: `compute_calibrated_score_distribution`, `find_latest_scoring_date_with_calibration`, `validate_snapshot_compatibility`, `compare_distributions`) and `scripts/check_calibrator_acceptance.py` CLI with `snapshot` and `check` subcommands. Three-tier output (PASS / PASS-with-WARNING at ≥70% of threshold / FAIL); exit codes 0 / 0 / 1 / 2 (INSUFFICIENT_DATA when baseline date has no current rows or count < `min_sample_size`). Snapshot JSON stores per-ticker calibrated_score values so bipolar shifts (mean ≈ 0 but per-ticker huge) surface in the report; `tickers_exceeding_delta` is informational, not blocking. Telegram send wrapped in try/except so a Telegram outage cannot change exit code; `.env` loaded via `load_dotenv()`. Strict baseline-date enforcement: `check` derives scoring_date from `baseline.scoring_date` only — never falls back to a fresh DB query. Added `calibrator_acceptance` config block to `scorer.json` with rationale comments in CONFIG.md. 28 new tests (11 module-level + 17 CLI-level via `monkeypatch.setattr(sys, 'argv', ...)` + SystemExit; Telegram patched in all CLI tests). 368/368 scorer + scripts tests pass. CONFIG.md, OPERATIONS.md (Flipping weekly_score_method procedure), DESIGN.md (12b Calibrator Acceptance Gate), DEVELOPMENT.md updated. | ✅ Done |
| 2026-04-28 | Commit 6 of weekly/monthly parity: scorer persistence to `scores_weekly` + `scores_monthly` with closed-period gate. New `src/scorer/persistence.py` (`persist_weekly_score_row`, `persist_monthly_score_row`, `_inherit_fundamental_macro`) and `src/scorer/period_gate.py` (`is_week_closed`, `is_month_closed`). `score_ticker` now calls the breakdown API directly so the full per-category dict reaches the persist helpers. Closed-period gate: weekly closes when `scoring_date >= week_start + 7 days`; monthly closes on month rollover. **B1 schema bug fix**: `scores_weekly`/`scores_monthly`.`data_completeness` REAL → TEXT in `db.py` + new idempotent `migrate_fix_scores_completeness_type.py`. 50 new tests; 1221 non-notifier tests pass. | ✅ Done |

## Key Decisions
<!-- Recent trade-offs and choices that affect future work -->
- **Daily card enriched with `key_signals` top-3, earnings row, and signal-flip badge (2026-04-29).** Adversary dropped market-context banner (QQQ-only chrome — macro already in category bars) and news feed (sentiment ≈ 0, headlines clickbait-quality, no click-through). All three picks are daily-only; weekly/monthly cards are unchanged. Signal-flip `id DESC` tiebreaker is mandatory due to production duplicates (ASTS ×3 same-date, LLY ×2 contradictory same-date). Earnings `actual_eps IS NULL` guard excludes 18 stale-null past rows.
- **Per-timeframe data parity (commits 1-9)**: weekly + monthly now mirror daily's event tables (patterns, divergences, crossovers, swing_points, S/R, profiles) + score breakdown. Schema chose **Option A** (separate `scores_weekly` + `scores_monthly` tables keyed by week_start/month_start) over extending `scores_daily`. UI rendering chose **Option C** (per-timeframe score + categorical breakdown only — NO discrete BUY/SELL signal classification per timeframe; the merged daily signal remains the single decision the engine publishes).
- **`weekly_score_method` / `monthly_score_method` = `v2_8cat` in production (flipped 2026-04-29)**. v2_8cat loads weekly/monthly events from the mirror tables and routes crossovers→trend, divergences→momentum/volume, candlestick→cdl, structural→struct (cdl/struct weights currently 0.0, so the v2 scalar differs from v1 only via trend/momentum/volume getting weekly events). Flip was gated by a shadow acceptance gate run that PASSED with mean_delta=+0.004, std_delta=−0.006 — orders of magnitude inside thresholds. Reverting requires the same gate procedure in reverse (snapshot, flip back, re-historical, check).
- **scores_weekly/_monthly are denormalized snapshots** for query/UI — NOT in the scoring critical path. `merge_timeframes()` consumes the in-memory composite from `compute_*_score_breakdown()`.
- **Closed-period gate**: weekly closes when `scoring_date >= week_start + 7 days` (Sunday is in-progress; Monday closes the prior week). Monthly closes when `(scoring_date.year, .month) > (month_start.year, .month)`. Persistence skips in-progress periods; live merged signal uses partial bars.
- **Monthly candlestick scoring is permanently disabled** (decay-window mismatch — `_CANDLESTICK_WINDOW_DAYS=7` zeroes out monthly bars). `candlestick_score = None` in monthly breakdown regardless of v1/v2 mode.
- **`weekly_score` is in the LLM prompt context** (`ai_reasoner.py:803`), `monthly_score` is NOT. Flipping v2 will subtly shift Telegram blurb tone via the LLM input shift; mitigated by manual pre/post diff in the flip procedure.
- **Calibrator acceptance gate** uses distribution-level thresholds (max_mean_delta=5.0, max_std_delta=8.0 on calibrated_score across all tickers at a fixed scoring_date) rather than R²-based thresholds. Stores per-ticker values in baseline JSON to surface bipolar shifts. Three-tier output (PASS / PASS-with-WARNING ≥70% of threshold / FAIL).
- **Monthly timeframe added** (pre-parity): 3-way merge with regime-adaptive weights (trending 0.10d/0.50w/0.40m, ranging 0.60d/0.30w/0.10m, volatile 0.25d/0.45w/0.30m). When monthly is absent, remaining weights are renormalized to sum to 1.0.
- **Calibrator 17 features**: 6 category + 6 raw + 3 EMA spreads + weekly_score + monthly_score. Rolling ridge: window=365, lambda=0.1. Validated R²=0.1025.
- **Confidence base uses calibrated_score** (warm): `min(abs(cal), 8.0) * 10`. Cold start: `abs(final_score) * 0.3`.
- **`final_score` column is always ±100** (merged composite). `calibrated_score` (≈ ±2–15%) is separate.

## Next Up
<!-- Known upcoming tasks or follow-ups -->
- **~2027-04-29 (365 days post-v2 flip)**: re-run the calibrator acceptance gate with a fresh baseline. By then the 365-day training window will be fully v2-scored, so the readout will no longer be contaminated by mixed-semantics training rows. Until then, day-to-day signal output is correct but the gate would only validate consistency at this fixed moment.
- **Investigate the lone verify_pipeline warning**: `monthly_divergence_count: 0 divergences_monthly rows in last 12 months`. Pre-existing, unrelated to v2; either the detection thresholds for monthly are too strict or no qualifying divergences have formed in the active universe over the lookback.
- Monitor anti-predictive tickers and BEARISH asymmetry (previously flagged improvement area).
